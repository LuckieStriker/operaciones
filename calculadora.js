
document.getElementById ("suma", "resta", "mult", "divi");


const suma = (a,b)=> a+b;
const resta= (a,b)=> a-b;
const mult= (a,b)=> a*b;
const divi= (a,b)=> a/b;

switch (calc){
    case "+": (suma);
    case "-": (resta);
    case "*": (mult);
    case "/": (divi);

    return calc;
}
